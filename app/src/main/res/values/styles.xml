<resources>
    <!-- Base application theme -->
    <style name="Theme.YourApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary branding color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <!-- Secondary branding color. -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryVariant</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle">@style/CustomButton</item>
    </style>

    <!-- Custom button style -->
    <style name="CustomButton" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/rounded_button</item>
        <item name="backgroundTint">@color/colorButton</item>
        <item name="textAllCaps">false</item>
    </style>

    <!-- Extra button style for the four buttons -->
    <style name="CustomButton.Transparent" parent="CustomButton">
        <item name="android:background">@drawable/button_background_transparent</item>
        <item name="android:textColor">#000000</item>
        <item name="backgroundTint">#B6B1B1</item>
    </style>

    <!-- Badge Style for Notifications -->
    <style name="Badge.Text" parent="Widget.MaterialComponents.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/badge_background</item>
        <item name="backgroundTint">#F40418</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingLeft">4dp</item>
        <item name="android:paddingRight">4dp</item>
        <item name="android:paddingTop">2dp</item>
        <item name="android:paddingBottom">2dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:gravity">center</item>
    </style>
</resources>
